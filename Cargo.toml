[package]
name = "mini-os"
version = "0.1.0"
edition = "2024"

[dependencies]

[profile.dev]
panic = "abort" # comments work

[profile.release]
panic = "abort" # both aborts disable stack unwinding on panic